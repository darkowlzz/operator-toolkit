// Package v1 contains helpers to build a composite controller reconciler.
// It defines a Controller interface which provides methods required for
// reconciling a composite controller. It also provides an implementation of
// the Reconcile method that can be embedded in a controller to satisfy the
// controller-runtime's Reconciler interface.
package v1
